/* 32. Local Storage (browser only) */
// localStorage lets you store data in the browser that persists after reloads.
// Only works in browsers, not Node.js.
// Save data:
// localStorage.setItem("username", "Alice");
// Get data:
// let user = localStorage.getItem("username");
// console.log("Username from localStorage:", user);
// Remove data:
// localStorage.removeItem("username");
